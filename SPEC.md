# VaultX - 本地密码保险箱

## 1. 项目概述

**项目名称**: VaultX  
**类型**: 桌面应用（Electron + React + TypeScript）  
**核心功能**: 本地加密存储各类敏感信息（密码、银行卡、API密钥、个人信息等）  
**目标用户**: 需要安全存储敏感信息、注重隐私的国内用户

## 2. UI/UX 规格

### 2.1 窗口模型
- **主窗口**: 900x650，最小 700x500，可调整大小
- **模态对话框**: 添加/编辑条目、设置、密码生成器
- **系统托盘**: 最小化到托盘，支持快速锁定

### 2.2 视觉设计

**色彩系统**（暗色主题为主）:
- 主背景: `#0D0D0F` (深黑)
- 次背景: `#18181B` (卡片背景)
- 强调色: `#6366F1` (靛蓝)
- 成功: `#22C55E`
- 警告: `#F59E0B`
- 危险: `#EF4444`
- 文字主色: `#FAFAFA`
- 文字次色: `#A1A1AA`
- 边框: `#27272A`

**字体**:
- 标题: "Noto Sans SC", 700 weight
- 正文: "Noto Sans SC", 400 weight
- 代码/密码: "JetBrains Mono"

**间距**: 8px 基准网格 (4, 8, 12, 16, 24, 32, 48)

**动效**:
- 页面切换: 200ms ease-out 滑动
- 按钮悬停: 150ms 缩放 1.02
- 卡片悬停: 200ms 边框发光
- 密码显示: 150ms 淡入淡出

### 2.3 布局结构

```
┌─────────────────────────────────────────────────────────┐
│  [Logo] VaultX          [搜索框]        [+] [⚙] [🔒]  │  <- 顶部栏 (56px)
├──────────────┬──────────────────────────────────────────┤
│              │                                          │
│  侧边栏      │              主内容区                     │
│  (220px)     │                                          │
│              │  ┌────────┐ ┌────────┐ ┌────────┐        │
│  • 全部      │  │  卡片   │ │  卡片  │ │  卡片  │        │
│  • 登录密码  │  │        │ │        │ │        │        │
│  • 银行卡    │  │        │ │        │ │        │        │
│  • API密钥   │  └────────┘ └────────┘ └────────┘        │
│  • 个人信息  │                                          │
│  • 安全笔记  │                                          │
│              │                                          │
├──────────────┴──────────────────────────────────────────┤
│  共 X 项                              [导入] [导出]     │  <- 底部栏 (40px)
└─────────────────────────────────────────────────────────┘
```

### 2.4 组件列表

1. **顶部栏**: Logo、搜索框、添加按钮、设置、锁定
2. **侧边栏**: 分类导航、分类计数、选中态
3. **条目卡片**: 图标、标题、用户名/卡号掩码、复制按钮、编辑、删除
4. **条目详情**: 完整信息展示、显示/隐藏密码、复制全部
5. **添加/编辑表单**: 分类选择、字段动态添加、密码生成器
6. **密码生成器**: 长度滑块、选项（大小写、数字、符号）、生成按钮、复制
7. **设置面板**: 修改主密码、自动锁定时间、备份/恢复
8. **锁屏**: 主密码输入、指纹/面部（可选）

## 3. 功能规格

### 3.1 核心功能

**3.1.1 加密引擎**
- 算法: AES-256-GCM
- 密钥派生: PBKDF2 (600,000 轮, SHA-512)
- 盐值: 32 字节随机
- IV: 12 字节随机（每次加密不同）
- 存储格式: JSON -> AES加密 -> Base64

**3.1.2 数据分类**
| 分类 | 字段 |
|------|------|
| 登录密码 | 标题, 网站/应用, 用户名, 密码, URL, 备注 |
| 银行卡 | 标题, 银行名称, 卡号, 持卡人, 有效期, CVN, 手机号, 备注 |
| API密钥 | 标题, 服务名称, API Key, Secret Key, 额外参数, 备注 |
| 个人信息 | 标题, 姓名, 邮箱, 手机, 身份证, 地址, 备注 |
| 安全笔记 | 标题, 内容（富文本） |

**3.1.3 密码生成器**
- 长度: 8-64 位（默认 16）
- 选项: 大写字母、小写字母、数字、符号
- 排除相似字符（0O1lI）
- 实时强度评估

**3.1.4 搜索与过滤**
- 实时搜索（标题、用户名、备注）
- 按分类筛选
- 排序（名称、最近修改）

**3.1.5 自动锁定**
- 空闲超时: 1/5/15/30 分钟（默认 5）
- 最小化到托盘
- 快捷键锁定

**3.1.6 导入/导出**
- 导出: 加密的 .vaultx 文件
- 导入: .vaultx 文件（需验证密码）
- 支持 CSV 导入（仅密码类）

### 3.2 用户流程

1. **首次启动**: 创建主密码 → 确认主密码 → 进入主界面
2. **日常使用**: 输入主密码 → 解锁保险箱 → 查看/操作条目
3. **添加条目**: 点击+ → 选择分类 → 填写字段 → 保存
4. **自动锁定**: 超时/点击锁定 → 回到锁屏

### 3.3 数据存储

- 位置: `%APPDATA%/VaultX/vault.data`
- 格式: AES-256-GCM 加密的 JSON
- 备份: 手动导出到指定位置

## 4. 验收标准

### 4.1 功能验收
- [ ] 能创建主密码并成功解锁
- [ ] 能添加/编辑/删除各分类条目
- [ ] 密码生成器能生成符合要求的密码
- [ ] 搜索功能正常工作
- [ ] 自动锁定功能正常
- [ ] 数据能正确加密存储
- [ ] 导入/导出功能正常

### 4.2 视觉验收
- [ ] 暗色主题正确显示
- [ ] 卡片悬停有发光效果
- [ ] 页面切换有滑动动画
- [ ] 密码显示/隐藏有过渡动画
- [ ] 响应式布局正常（最小 700x500）

### 4.3 安全验收
- [ ] 关闭应用后内存中无明文密码
- [ ] 加密文件无法被普通工具读取
- [ ] 主密码错误无法解锁
